int totalScore = 0;

void AddScore(int currentScore, int points)
{
    currentScore += points;
    
}


AddScore(totalScore,10);
Console.WriteLine("Total score is now: " + totalScore);
AddScore(totalScore,20);
Console.WriteLine("Total score is now: " + totalScore);