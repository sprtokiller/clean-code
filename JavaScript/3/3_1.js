const LONG_TERM_SERVICE_THRESHOLD = 10;
if (employee.yearsOfService > LONG_TERM_SERVICE_THRESHOLD) {
  console.log("Eligible for long-term service award.");
} else {
  console.log("Not eligible for award.");
}
